/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['d7hftxdivxxvm.cloudfront.net',"d32dm0rphc51dk.cloudfront.net"], // Add other domains if needed
      }

};

export default nextConfig;
